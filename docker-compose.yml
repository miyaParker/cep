version: '3.8'
services:
    web:
        build:
            context: .
        command: npm start
        ports:
            - '${PORT}:${PORT}'
        environment:
            NODE_ENV: development
            PORT: ${PORT}
