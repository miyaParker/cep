"use client"
import SubscribeForm from "@/components/LandingPage/SubscribeForm";
import ProjectsList from "@/components/ProjectsPage/ProjectsList";
import {Dispatch, SetStateAction} from "react";

const Projects = (props:any) => {
    console.log("props",props)

    return (
        <div>projects</div>
        // <div className={`${isModalOpen ? "bg-[#111111]/25" : ""}`}>
        //     <ProjectsList setIsModalOpen={setIsModalOpen} isModalOpen={isModalOpen}/>
        //     <SubscribeForm/>
        // </div>
    );
};
export default Projects;
